/*!
 * base.main
 */

(function( tiko, $, ko, underscore ) { 'use strict';

  // encapsulating base libraries
  // for easier access
  tiko.$ = $;
  tiko.ko = ko;
  tiko._ = underscore;

})( this.tiko = this.tiko || {}, jQuery, ko, _ );